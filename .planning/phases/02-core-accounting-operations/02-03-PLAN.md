---
phase: 02-core-accounting-operations
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02-PLAN.md
files_modified:
  - backend/services/retenciones.py
  - backend/agents/contador/handlers/egresos.py
  - backend/tests/test_phase2_wave3.py
autonomous: true
requirements:
  - EGRE-01
  - EGRE-02
  - EGRE-03
  - EGRE-04
  - CONC-04

must_haves:
  truths:
    - "calcular_retenciones('arriendo', 3614953, None) returns correct retefuente 3.5% and reteica 0.414%"
    - "calcular_retenciones with nit='860024781' (Auteco) always returns retefuente_monto=0"
    - "calcular_retenciones('compras', 1000000, None) returns retefuente_monto=0 (below threshold)"
    - "calcular_retenciones('compras', 2000000, None) returns retefuente_monto=50000 (2.5%)"
    - "handle_crear_causacion validates balanced entries before posting to Alegra"
    - "handle_registrar_gasto with proveedor_nit=860024781 posts zero retefuente to Alegra"
    - "handle_registrar_gasto with CC 80075452 or 80086601 posts to CXC account not gasto operativo"
    - "handle_anular_causacion does GET-verify, DELETE, GET-confirm-404 three-step sequence"
    - "All 7 egresos handlers publish events to event_bus after successful Alegra write"
    - "Zero direct MongoDB writes in egresos.py (only via event_bus.publish for roddos_events)"
  artifacts:
    - path: "backend/services/retenciones.py"
      provides: "calcular_retenciones() pure function with Colombia 2026 rates"
      exports:
        - calcular_retenciones
        - AUTORETENEDORES
        - TASAS_RETEFUENTE
    - path: "backend/agents/contador/handlers/egresos.py"
      provides: "7 egresos handler functions"
      exports:
        - handle_crear_causacion
        - handle_registrar_gasto
        - handle_registrar_gasto_recurrente
        - handle_anular_causacion
        - handle_causar_movimiento_bancario
        - handle_registrar_ajuste_contable
        - handle_registrar_depreciacion
  key_links:
    - from: "backend/agents/contador/handlers/egresos.py"
      to: "backend/services/retenciones.py"
      via: "calcular_retenciones(tipo, monto, nit)"
      pattern: "calcular_retenciones"
    - from: "backend/agents/contador/handlers/egresos.py"
      to: "Alegra POST /journals"
      via: "alegra.request_with_verify('journals', 'POST', body=payload)"
      pattern: "request_with_verify.*journals"
---

<objective>
Implement the retenciones service and 7 egresos handlers — the core contable write path.

Purpose: Every expense in RODDOS flows through these handlers. Retenciones must be correct per Colombian law 2026 before any journal hits Alegra. Auteco must never get ReteFuente.
Output: retenciones.py (pure function, no I/O), egresos.py (7 handlers calling request_with_verify).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-accounting-operations/02-CONTEXT.md
@.planning/phases/phase-2-core-accounting/SISMO_V2_Phase2_CONTEXT.md
@.planning/SISMO_V2_Registro_Canonico.md

<interfaces>
<!-- Retenciones rates — Colombia 2026 (from CLAUDE.md and CONTEXT.md) -->
TASAS_RETEFUENTE:
  arriendo: 3.5%
  servicios: 4%
  honorarios_pn: 10% (persona natural)
  honorarios_pj: 11% (persona juridica)
  compras: 2.5% (ONLY if monto > 1,344,573)
RETEICA_BOGOTA: 0.414% always applied
AUTORETENEDORES: {"860024781"} — Auteco, retefuente always 0
COMPRAS_BASE_MINIMA: 1,344,573

Alegra account IDs (from Registro Canonico):
  ReteFuente practicada = 236505
  ReteICA practicada = 236560
  Gastos Generales FALLBACK = 5493 (NUNCA 5495)
  Arrendamientos = 5480
  Servicios publicos = 5484
  Honorarios = 5470
  Bancos: Bancolombia=111005, BBVA=111010, Davivienda=111015, Bogota=111020, Global66=11100507

CXC Socios:
  Andres Sanjuan CC 80075452 — CXC Socio (never gasto operativo)
  Ivan Echeverri CC 80086601 — CXC Socio (never gasto operativo)

Handler signature (all egresos handlers):
async def handle_X(tool_input: dict, alegra: AlegraClient, db: AsyncIOMotorDatabase,
                   event_bus: EventPublisher, user_id: str) -> dict

Write pattern (mandatory for all write handlers):
  1. validate_write_permission("contador", "POST /journals", "alegra")
  2. Build balanced entries (sum debits == sum credits — validate before calling Alegra)
  3. result = await alegra.request_with_verify("journals", "POST", body=payload)
  4. await event_bus.publish(event_type=..., source="agente_contador", datos={...})
  5. Return {"success": True, "alegra_id": result["_alegra_id"], "message": "..."}

Alegra journal payload format:
{
    "date": "2026-04-09",          # yyyy-MM-dd — NUNCA ISO-8601
    "observations": "descripcion",
    "entries": [
        {"account": {"id": 5480}, "debit": 3614953, "credit": 0},
        {"account": {"id": 111010}, "debit": 0, "credit": 3473464},
        {"account": {"id": 236505}, "debit": 0, "credit": 126524},
        {"account": {"id": 236560}, "debit": 0, "credit": 14965},
    ]
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retenciones service — pure function with Colombia 2026 rates</name>
  <files>
    backend/services/retenciones.py
    backend/tests/test_phase2_wave3.py
  </files>
  <behavior>
    - Test 1: calcular_retenciones("arriendo", 3_614_953, None) returns retefuente_tasa=0.035, retefuente_monto=126523.36, reteica_monto=14965.79, neto_a_pagar=3_473_463.85
    - Test 2: calcular_retenciones("servicios", 1_000_000, None) returns retefuente_monto=40000, reteica_monto=4140
    - Test 3: calcular_retenciones("honorarios_pn", 500_000, None) returns retefuente_monto=50000
    - Test 4: calcular_retenciones("honorarios_pj", 500_000, None) returns retefuente_monto=55000
    - Test 5: calcular_retenciones("compras", 1_000_000, None) returns retefuente_monto=0 (below base)
    - Test 6: calcular_retenciones("compras", 2_000_000, None) returns retefuente_monto=50000
    - Test 7: calcular_retenciones("arriendo", 5_000_000, "860024781") returns retefuente_monto=0 (Auteco)
    - Test 8: calcular_retenciones("servicios", 1_000_000, "860024781") returns reteica_monto=4140 (ReteICA still applies)
    - Test 9: neto_a_pagar == monto - retefuente_monto - reteica_monto always
    - Test 10: Unknown tipo returns retefuente_monto=0, only reteica applied
  </behavior>
  <action>
Write tests first in backend/tests/test_phase2_wave3.py (tests 1-10), run pytest to confirm RED, then implement.

Create backend/services/retenciones.py:

```python
"""
Retenciones Colombia 2026 — pure function, no I/O.

Decisions applied (CONTEXT.md D-08 to D-12):
- D-09: Auteco NIT 860024781 = autoretenedor, retefuente always 0
- D-10: ReteICA Bogota 0.414% always applied
- D-11: Compras ReteFuente 2.5% only if monto > $1,344,573
- D-12: Handlers import this module — never calculate inline
"""

AUTORETENEDORES: frozenset[str] = frozenset({"860024781"})

TASAS_RETEFUENTE: dict[str, float] = {
    "arriendo": 0.035,
    "servicios": 0.04,
    "honorarios_pn": 0.10,
    "honorarios_pj": 0.11,
    "compras": 0.025,
}

RETEICA_BOGOTA: float = 0.00414
COMPRAS_BASE_MINIMA: float = 1_344_573.0


def calcular_retenciones(
    tipo: str,
    monto: float,
    nit: str | None = None,
) -> dict:
    es_autoretenedor = nit in AUTORETENEDORES if nit else False

    tasa_retefuente = TASAS_RETEFUENTE.get(tipo, 0.0)
    if es_autoretenedor:
        retefuente_monto = 0.0
        tasa_retefuente = 0.0
    elif tipo == "compras" and monto <= COMPRAS_BASE_MINIMA:
        retefuente_monto = 0.0
        tasa_retefuente = 0.0
    else:
        retefuente_monto = round(monto * tasa_retefuente, 2)

    reteica_monto = round(monto * RETEICA_BOGOTA, 2)
    neto_a_pagar = round(monto - retefuente_monto - reteica_monto, 2)

    return {
        "retefuente_tasa": tasa_retefuente,
        "retefuente_monto": retefuente_monto,
        "reteica_tasa": RETEICA_BOGOTA,
        "reteica_monto": reteica_monto,
        "neto_a_pagar": neto_a_pagar,
    }
```

Run: pytest backend/tests/test_phase2_wave3.py -k "retencion" -v
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO-V2 && python -m pytest backend/tests/test_phase2_wave3.py -k "retencion" -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    - All 10 retenciones tests pass GREEN
    - calcular_retenciones is a pure function (no async, no I/O, no side effects)
    - Auteco (NIT 860024781) always gets retefuente_monto=0
    - Compras below $1,344,573 always gets retefuente_monto=0
    - ReteICA always applied regardless of autoretenedor status
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 7 egresos handlers</name>
  <files>
    backend/agents/contador/handlers/egresos.py
    backend/tests/test_phase2_wave3.py
  </files>
  <behavior>
    - Test 11: handle_crear_causacion calls validate_write_permission("contador", "POST /journals", "alegra") before Alegra call
    - Test 12: handle_crear_causacion posts payload to request_with_verify("journals", "POST") and publishes "gasto.causado" event
    - Test 13: handle_crear_causacion returns {"success": True, "alegra_id": 12345, "message": "Journal #12345 creado en Alegra..."}
    - Test 14: handle_registrar_gasto banco="Bancolombia" uses credit account id 111005; "BBVA"->111010; "Davivienda"->111015
    - Test 15: handle_registrar_gasto proveedor_nit="860024781" posts zero retefuente credit entry (Auteco)
    - Test 16: handle_registrar_gasto with descripcion containing "80075452" posts to CXC account and publishes "cxc.socio.registrada"
    - Test 17: handle_anular_causacion calls GET /journals/{id}, DELETE /journals/{id}, then GET again to confirm 404
    - Test 18: All posted journal entry payloads have sum(debits) == sum(credits)
    - Test 19: handle_crear_causacion with unbalanced entries returns {"success": False, "error": "Asiento desbalanceado"} without calling Alegra
    - Test 20: STATIC — grep for insert_one/update_one/insert_many/replace_one in egresos.py returns 0 matches
  </behavior>
  <action>
Add tests 11-20 to test_phase2_wave3.py, confirm RED, then implement.
Use unittest.mock.AsyncMock for AlegraClient and EventPublisher.

Create backend/agents/contador/handlers/egresos.py with these 7 functions:

handle_crear_causacion(tool_input, alegra, db, event_bus, user_id):
  - tool_input: {entries: [{id, debit, credit}], date: "yyyy-MM-dd", observations: str}
  - Validate balanced: round(sum(e["debit"] for e in entries), 2) == round(sum(e["credit"] for e in entries), 2)
  - If unbalanced: return {"success": False, "error": "Asiento desbalanceado: debitos != creditos"}
  - validate_write_permission("contador", "POST /journals", "alegra")
  - payload = {"date": tool_input["date"], "observations": tool_input["observations"],
               "entries": [{"account": {"id": e["id"]}, "debit": e["debit"], "credit": e["credit"]} for e in entries]}
  - result = await alegra.request_with_verify("journals", "POST", body=payload)
  - await event_bus.publish("gasto.causado", "agente_contador", {"alegra_id": result["_alegra_id"], ...})
  - return {"success": True, "alegra_id": result["_alegra_id"], "message": f"Journal #{result['_alegra_id']} creado en Alegra. {observations}"}

handle_registrar_gasto(tool_input, alegra, db, event_bus, user_id):
  - tool_input: {descripcion, monto, banco, fecha (opt), proveedor_nit (opt), tipo_persona (opt)}
  - CXC check: socios_cc = {"80075452": "Andrés Sanjuan", "80086601": "Iván Echeverri"}
    if any CC in tool_input.get("proveedor_nit","") or descripcion:
      route to CXC logic: debit=CXC_account, credit=banco_account, event="cxc.socio.registrada"
      (Use account 1305 placeholder — read plan_cuentas_roddos from db for exact ID)
  - Normal path: classify tipo from descripcion:
      "arriend" -> tipo="arriendo", cuenta_gasto=5480
      "honorar" -> tipo="honorarios_pn" or "honorarios_pj" based on tipo_persona, cuenta_gasto=5470
      "servicio" -> tipo="servicios", cuenta_gasto=5484
      default -> tipo="servicios", cuenta_gasto=5493 (FALLBACK — NUNCA 5495)
  - from services.retenciones import calcular_retenciones
  - ret = calcular_retenciones(tipo, monto, tool_input.get("proveedor_nit"))
  - banco_ids = {"Bancolombia": 111005, "BBVA": 111010, "Davivienda": 111015, "Banco de Bogota": 111020, "Global66": 11100507}
  - banco_id = banco_ids.get(tool_input["banco"], 111005)
  - fecha = tool_input.get("fecha") or datetime.date.today().isoformat()
  - entries = [
      {"id": cuenta_gasto, "debit": monto, "credit": 0},
      {"id": banco_id, "debit": 0, "credit": ret["neto_a_pagar"]},
    ]
    if ret["retefuente_monto"] > 0:
      entries.append({"id": 236505, "debit": 0, "credit": ret["retefuente_monto"]})
    if ret["reteica_monto"] > 0:
      entries.append({"id": 236560, "debit": 0, "credit": ret["reteica_monto"]})
  - Call _post_journal(entries, fecha, descripcion, alegra) helper
  - event: "gasto.causado"

handle_registrar_gasto_recurrente(tool_input, alegra, db, event_bus, user_id):
  - tipo_gasto->tipo mapping: arriendo->arriendo/5480, servicios_publicos->servicios/5484,
    telefonia->servicios/5487, seguros->servicios/5510, mantenimiento->servicios/5490
  - Same retenciones + journal pattern as registrar_gasto
  - event: "gasto.causado"

handle_anular_causacion(tool_input, alegra, db, event_bus, user_id):
  - tool_input: {journal_id: int, motivo: str}
  - validate_write_permission("contador", "DELETE /journals", "alegra")
  - GET /journals/{journal_id} — if not found return error
  - DELETE /journals/{journal_id} via request_with_verify
  - GET /journals/{journal_id} again — should be 404 (verify deletion)
  - event: "cleanup.journals.ejecutado"
  - return {"success": True, "anulado_id": journal_id, "motivo": motivo}

handle_causar_movimiento_bancario: same logic as registrar_gasto

handle_registrar_ajuste_contable(tool_input, ...):
  - 2-entry journal: DEBIT cuenta_destino / CREDIT cuenta_origen
  - event: "gasto.causado"

handle_registrar_depreciacion(tool_input, ...):
  - DEBIT 5493 (depreciacion fallback) / CREDIT activo account (use 5493 as both if unknown)
  - event: "gasto.causado"

Private helper: _post_journal(entries, fecha, observaciones, alegra):
  Validates balance, builds payload, calls request_with_verify.

MANDATORY: validate_write_permission before every write.
MANDATORY: No insert_one/update_one/insert_many/replace_one in this file.
MANDATORY: fecha always in yyyy-MM-dd format (use datetime.date.today().isoformat() as default).

Run: pytest backend/tests/test_phase2_wave3.py -v
  </action>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO-V2 && python -m pytest backend/tests/test_phase2_wave3.py -v && grep -rn "insert_one\|insert_many\|update_one\|replace_one" backend/agents/contador/handlers/egresos.py 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All 20 tests pass (10 retenciones + 10 egresos)
    - grep for MongoDB write ops in egresos.py returns 0 lines
    - All Phase 1 tests (76) + Wave 1 + Wave 2 continue passing
    - Auteco gets retefuente=0; socios route to CXC
    - Entries always balance before Alegra POST
    - Commit: "phase2-wave3: 7 egresos handlers con retenciones automaticas"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool_input -> Alegra journal entries | LLM-provided monto, cuenta_id, fecha go into Alegra journal |
| LLM NIT field -> autoretenedor lookup | LLM claims NIT is autoretenedor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-W3-01 | Tampering | NIT autoretenedor bypass | mitigate | AUTORETENEDORES is frozenset in code — LLM cannot add NITs at runtime |
| T-02-W3-02 | Tampering | Unbalanced journal entries | mitigate | _validate_entries_balance() blocks unbalanced payloads before Alegra call |
| T-02-W3-03 | Tampering | Date format injection | mitigate | fecha is validated as yyyy-MM-dd; default is datetime.date.today().isoformat() |
| T-02-W3-04 | Elevation of Privilege | socio CC in descripcion bypass | mitigate | CXC check runs on both proveedor_nit and descripcion fields |
| T-02-W3-05 | Repudiation | gasto without event | mitigate | event_bus.publish called after every successful Alegra write |
| T-02-W3-06 | Information Disclosure | Alegra credentials in error | accept | Errors return message string only — no credentials exposed |
</threat_model>

<verification>
After Wave 3 completes:

1. pytest backend/tests/test_phase2_wave3.py -v -> 20 tests GREEN
2. pytest backend/tests/ -v -> 76 + 10 + 9 + 20 = all GREEN
3. grep -rn "insert_one\|insert_many\|update_one\|replace_one" backend/agents/contador/handlers/ | grep -v "roddos_events\|conciliacion_jobs\|inventario_motos\|loanbook" -> 0 lines
4. grep -rn "5495" backend/ -> 0 results
5. grep -rn "journal-entries" backend/ -> 0 results
6. python -c "from services.retenciones import calcular_retenciones; r = calcular_retenciones('arriendo', 3614953, '860024781'); assert r['retefuente_monto'] == 0, 'Auteco bypass failed'"
</verification>

<success_criteria>
- calcular_retenciones() is importable as pure function with no dependencies
- 7 egresos handlers all follow validate->build->request_with_verify->publish->return pattern
- Auteco NIT 860024781 always produces retefuente_monto=0
- Socios CC 80075452 and CC 80086601 always route to CXC (not gasto operativo)
- Unbalanced entries blocked before Alegra call
- Zero MongoDB write calls in egresos.py (only event_bus.publish for bus events)
- All previous tests continue passing (zero regressions)
- Commit: "phase2-wave3: 7 egresos handlers con retenciones automaticas"
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-accounting-operations/02-03-SUMMARY.md`
</output>
