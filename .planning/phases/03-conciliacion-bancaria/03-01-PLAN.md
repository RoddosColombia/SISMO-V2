---
phase: 03-conciliacion-bancaria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/bank_parsers.py
  - backend/services/anti_duplicados.py
  - backend/pyproject.toml
  - backend/tests/test_phase3_wave1.py
autonomous: true
requirements:
  - CONC-01
  - CONC-03

must_haves:
  truths:
    - "detect_bank() returns 'nequi' for .pdf files"
    - "detect_bank() returns 'bancolombia' for .xlsx with sheet 'Extracto' and headers at row 15"
    - "detect_bank() returns 'bbva' for .xlsx with headers at row 14"
    - "detect_bank() returns 'davivienda' for .xlsx with skiprows=4 format"
    - "parse_bancolombia returns list of dicts with fecha, descripcion, monto, tipo (debito/credito)"
    - "parse_nequi uses pdfplumber to extract table from PDF"
    - "hash_extracto returns consistent MD5 for same file"
    - "hash_movimiento returns consistent MD5 for same fecha+descripcion+monto"
    - "check_extracto_duplicado returns True for previously processed extract"
    - "check_movimiento_duplicado returns True for previously processed movement"
  artifacts:
    - path: "backend/services/bank_parsers.py"
      provides: "4 bank parser functions + auto-detect"
      exports:
        - detect_bank
        - parse_bancolombia
        - parse_bbva
        - parse_davivienda
        - parse_nequi
    - path: "backend/services/anti_duplicados.py"
      provides: "3-layer anti-duplicate service"
      exports:
        - hash_extracto
        - hash_movimiento
        - check_extracto_duplicado
        - check_movimiento_duplicado
        - registrar_extracto_procesado
        - registrar_movimiento_procesado
---

<objective>
Build 4 bank parsers (Bancolombia/BBVA/Davivienda .xlsx + Nequi PDF) with auto-detection and 3-layer anti-duplicate service. These are pure data extraction functions with no Alegra writes.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bank parsers + auto-detect + anti-dup service</name>
  <files>
    backend/services/bank_parsers.py
    backend/services/anti_duplicados.py
    backend/pyproject.toml
    backend/tests/test_phase3_wave1.py
  </files>
  <read_first>
    - backend/pyproject.toml (add pdfplumber dependency)
    - .planning/SISMO_V2_CLAUDE.md (bank format details)
    - .planning/phases/03-conciliacion-bancaria/03-CONTEXT.md (D-01 through D-15)
  </read_first>
  <action>
1. Add pdfplumber to pyproject.toml dependencies
2. Create backend/services/bank_parsers.py:
   - detect_bank(file_path: str) -> str: .pdf=nequi, .xlsx auto-detect by headers
   - parse_bancolombia(file_path: str) -> list[dict]: sheet "Extracto", headers row 15, cols FECHA/DESCRIPCION/VALOR
   - parse_bbva(file_path: str) -> list[dict]: headers row 14, cols FECHA DE OPERACION/CONCEPTO/IMPORTE
   - parse_davivienda(file_path: str) -> list[dict]: skiprows=4, cols Fecha/Descripcion/Valor/Naturaleza
   - parse_nequi(file_path: str) -> list[dict]: pdfplumber, negative=egreso positive=ingreso
   - Each returns: [{fecha: "yyyy-MM-dd", descripcion: str, monto: float, tipo: "debito"|"credito"}]
3. Create backend/services/anti_duplicados.py:
   - hash_extracto(file_bytes: bytes) -> str: MD5 of entire file
   - hash_movimiento(fecha: str, descripcion: str, monto: float) -> str: MD5 of "fecha|descripcion|monto"
   - check_extracto_duplicado(db, hash: str) -> bool
   - check_movimiento_duplicado(db, hash: str) -> bool
   - registrar_extracto_procesado(db, hash: str, banco: str, movimientos: int)
   - registrar_movimiento_procesado(db, hash: str, alegra_id: str | None)
4. Write tests with inline sample data (no real files needed)
  </action>
  <acceptance_criteria>
    - backend/services/bank_parsers.py contains "def detect_bank"
    - backend/services/bank_parsers.py contains "def parse_bancolombia"
    - backend/services/bank_parsers.py contains "def parse_nequi"
    - backend/services/bank_parsers.py contains "import pdfplumber"
    - backend/services/anti_duplicados.py contains "def hash_extracto"
    - backend/services/anti_duplicados.py contains "def hash_movimiento"
    - backend/pyproject.toml contains "pdfplumber"
    - pytest backend/tests/test_phase3_wave1.py passes all tests
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO-V2 && python -m pytest backend/tests/test_phase3_wave1.py -v</automated>
  </verify>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-01 | Tampering | Malicious PDF upload | mitigate | pdfplumber only extracts text/tables, no code execution |
| T-03-02 | Tampering | Hash collision | accept | MD5 sufficient for dedup (not security), collision probability negligible for <1M records |
| T-03-03 | DoS | Large file upload | mitigate | File size limit on upload endpoint (10MB max) |
</threat_model>

<verification>
After Wave 1:
1. pytest backend/tests/test_phase3_wave1.py -v -> all GREEN
2. python -c "from services.bank_parsers import detect_bank; print('OK')" -> no import error
3. python -c "from services.anti_duplicados import hash_extracto; print('OK')" -> no import error
</verification>
