---
phase: 03-conciliacion-bancaria
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02-PLAN.md
files_modified:
  - frontend/src/pages/BacklogPage.tsx
  - frontend/src/components/BacklogBadge.tsx
  - backend/tests/test_phase3_integration.py
autonomous: true
requirements:
  - BACK-02
  - BACK-03

must_haves:
  truths:
    - "BacklogPage.tsx renders a table with columns: fecha, banco, descripcion, monto, razon, intentos"
    - "BacklogPage.tsx has filters for banco, fecha range, and razon"
    - "BacklogPage.tsx has a Causar button that opens a modal with cuenta selector and retenciones"
    - "BacklogBadge.tsx fetches count from GET /api/backlog/count and displays in sidebar"
    - "Integration tests verify: extract upload → parse → classify → cause/backlog routing"
    - "Integration tests verify: anti-dup blocks same extract twice"
    - "Integration tests verify: causar from backlog → journal in Alegra → exits backlog"
    - "All Phase 1 + Phase 2 tests still pass (zero regressions)"
  artifacts:
    - path: "frontend/src/pages/BacklogPage.tsx"
      provides: "Backlog management page with table, filters, and Causar modal"
    - path: "frontend/src/components/BacklogBadge.tsx"
      provides: "Sidebar badge showing pending backlog count"
    - path: "backend/tests/test_phase3_integration.py"
      provides: "End-to-end integration tests for conciliation flow"
---

<objective>
Build the Backlog UI (page + badge + Causar modal) and comprehensive integration tests for the full conciliation flow.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: BacklogPage.tsx + BacklogBadge.tsx</name>
  <files>
    frontend/src/pages/BacklogPage.tsx
    frontend/src/components/BacklogBadge.tsx
  </files>
  <read_first>
    - .planning/phases/03-conciliacion-bancaria/03-CONTEXT.md (D-18 backlog UI spec)
    - .planning/SISMO_V2_Fase0_Fase1.md (Capacidad 8: Backlog operativo spec)
  </read_first>
  <action>
1. Create frontend/src/pages/BacklogPage.tsx:
   - Fetch from GET /api/backlog with query params (banco, fecha_desde, fecha_hasta)
   - Table columns: Fecha, Banco, Descripcion, Monto, Razon Pendiente, Intentos, Acciones
   - Sort by fecha (oldest first)
   - Filters: dropdown Banco (Bancolombia/BBVA/Davivienda/Nequi), date range, razón text search
   - "Causar" button per row opens modal:
     - Muestra: descripcion, monto, banco, fecha
     - Select: cuenta contable (from catalogo_cuentas_roddos)
     - Optional: retenciones checkboxes
     - Confirm → POST /api/backlog/{id}/causar
     - On success: row disappears, toast "Movimiento causado — Journal #X"
     - On failure: toast with error

2. Create frontend/src/components/BacklogBadge.tsx:
   - Fetch GET /api/backlog/count
   - Display count in sidebar next to "Backlog" menu item
   - Poll every 60s for updates
   - Show 0 as no badge (hidden)
  </action>
  <acceptance_criteria>
    - frontend/src/pages/BacklogPage.tsx exists and contains "backlog"
    - frontend/src/components/BacklogBadge.tsx exists and contains "count"
    - BacklogPage has filter controls for banco and fecha
    - BacklogPage has a Causar modal with cuenta selector
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for full conciliation flow</name>
  <files>
    backend/tests/test_phase3_integration.py
  </files>
  <read_first>
    - backend/agents/contador/handlers/conciliacion.py
    - backend/services/bank_parsers.py
    - backend/services/anti_duplicados.py
    - backend/tests/test_phase2_integration.py (test pattern to follow)
  </read_first>
  <action>
Write backend/tests/test_phase3_integration.py with:

T1: parse_bancolombia returns correct structure from sample data
T2: parse_nequi handles negative/positive values correctly
T3: detect_bank returns correct banco for each format
T4: hash_extracto returns same hash for same content
T5: hash_movimiento returns same hash for same fecha+desc+monto
T6: Classification >= 0.70 routes to auto-cause
T7: Classification < 0.70 routes to backlog
T8: Anti-dup Capa 1: same extract hash → blocked
T9: Anti-dup Capa 2: same movement hash → skipped
T10: causar_desde_backlog → journal verified + exits backlog
T11: STATIC: no contable MongoDB writes in conciliacion.py (only backlog/conciliacion_jobs/roddos_events)
T12: All previous tests still pass (regression check)
  </action>
  <acceptance_criteria>
    - test_phase3_integration.py has at least 10 test functions
    - pytest backend/tests/test_phase3_integration.py passes all tests
    - pytest backend/tests/ passes all tests (zero regressions)
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/AndresSanJuan/roddos-workspace/SISMO-V2 && python -m pytest backend/tests/test_phase3_integration.py -v && python -m pytest backend/tests/ -q --tb=line 2>&1 | tail -5</automated>
  </verify>
</task>

</tasks>

<verification>
After Wave 3:
1. BacklogPage.tsx and BacklogBadge.tsx exist with required functionality
2. pytest backend/tests/test_phase3_integration.py -> all GREEN
3. pytest backend/tests/ -> ALL GREEN (zero regressions across Phase 1+2+3)
4. grep anti-MongoDB verification: 0 violations in conciliacion.py
</verification>
